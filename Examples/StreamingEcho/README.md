# Streaming Echo extension

The installable Web extension is `Extension/StreamingEcho.hoveryextension`. It has no native build step; copy that directory into Hovery's configured extensions directory.

Its labels are in `i18n.toml`, in English and Simplified Chinese; the page reads them from `extension.messages`.
